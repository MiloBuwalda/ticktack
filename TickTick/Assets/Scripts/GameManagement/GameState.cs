using UnityEngine;
using System.Collections;

/// <summary>
/// Enum for states instead of separate classes in xna
/// </summary>

public enum GameState {

	TitleMenu,
    LevelMenu,
    HelpMenu,
    PlayingState,
    LevelFinishedState,
    GameOverState
}
